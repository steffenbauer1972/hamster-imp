#Bedingte Anweisung

##Testbefehle

* `vornFrei()`
* `kornDa()`
* `maulLeer()`

##Bedingte Anweisung

	if( <boolescher Ausdruck> )
		Anweisung;

	z.B.:

	if( vornFrei() )
		vor();

	if( kornDa() )
		nimm();

	if( !maulLeer() )
		gib();
