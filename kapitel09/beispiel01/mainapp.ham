/*imperative program*/void main() {

	// bedingte Ausführung, zur Absicherung
	// unserer Grundbefehle
	
	if( vornFrei() )
		vor();
		
	if( kornDa() )
		nimm();
		
	if( ! maulLeer() )
		gib();    
}
